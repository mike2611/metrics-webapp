import { NavLink } from 'react-router-dom';

const Details = () => (
  <div>
    <NavLink className="nav-link" to="/">
      Go Back
    </NavLink>
    <p>Test Details component</p>
  </div>
);

export default (Details);
